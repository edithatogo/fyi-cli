Conductor review residual fixes. Added optimistic update_request_state_if_current to prevent stale remote state overwrites, structured JSON sync error output in the CLI, and tests for both. Verification: portable MSVC full fyi-core suite passed; 51 fyi-cli unit tests passed. Existing CLI subprocess integration tests overflow the stack only under direct portable-wrapper launching; repository CI remains green for normal subprocess execution.
