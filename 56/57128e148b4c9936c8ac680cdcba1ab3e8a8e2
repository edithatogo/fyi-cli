Task: Implement inline field editing with auto-save

Summary:
- Added a client-side RequestInlineEditor with controlled title, body, status, requester, URL, and tags fields.
- Added debounced auto-save via PATCH /api/requests/[id].
- Added a request update API route that validates payloads and forwards updates to the MCP client.
- Replaced the request detail server-action edit form with the auto-save editor.
- Added component coverage for debounce behavior and PATCH payload shape.

Modified files:
- dashboard/src/app/api/requests/[id]/route.ts
- dashboard/src/app/requests/[id]/page.tsx
- dashboard/src/components/RequestInlineEditor.tsx
- dashboard/src/components/RequestInlineEditor.test.tsx

Why:
- Phase 4.2 requires inline editing with auto-save so operators do not need a manual save action for routine request edits.

Verification:
- npm test -- RequestInlineEditor.test.tsx
- npm test
- npm run build
- npx tsc --noEmit
