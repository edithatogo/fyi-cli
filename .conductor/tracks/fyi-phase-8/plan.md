# Phase 8 plan

1. Extend snapshot parsing to summarize attachments and events.
2. Expose a per-request detail command in the CLI.
3. Add a richer request detail page in the local UI.
4. Add tests and update docs.
