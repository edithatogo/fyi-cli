{
  "$schema": "https://glama.ai/mcp/schemas/server.json",
  "maintainers": ["edithatogo"],
  "name": "fyi-mcp",
  "description": "FYI request management with tools for requests, authorities, correspondence, sync, and health.",
  "type": "mcp_server",
  "status": "beta",
  "framework": "Rust",
  "tools": 12,
  "transport": ["stdio"],
  "keywords": ["fyi", "mcp", "requests", "correspondence", "sync", "sqlite"],
  "dockerfile": "Dockerfile",
  "entrypoint": ["/usr/local/bin/fyi-mcp"],
  "environment": {
    "FYI_MCP_EPHEMERAL": {
      "description": "Use an in-memory SQLite database for Glama release tests and Try in Browser sessions.",
      "default": "1"
    },
    "DATABASE_URL": {
      "description": "SQLite database URL. The Glama container defaults to sqlite::memory:.",
      "default": "sqlite::memory:"
    }
  }
}
